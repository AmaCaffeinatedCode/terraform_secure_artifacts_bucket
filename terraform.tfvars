name = "terraform_secure_artifacts_bucket"

tags = {
  Environment = "production"
  Owner       = "infrastructure-team"
  Project     = "terraform_secure_artifacts_bucket"
}

# "project_url" will be set dinamically in the CI/CD pipeline